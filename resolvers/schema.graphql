type User {
  id: ID!
  name: String!
  email: String!
}

input CreateUserInput {
  id: ID!
  name: String!
  email: String!
}

type Mutation {
  createUser(input: CreateUserInput!): User
}

type Query {
  getUser(id: ID!): User
}

schema {
  query: Query
  mutation: Mutation
}
